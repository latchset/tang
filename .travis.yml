language: c
before_script:
  - autoreconf -if
script:
  - ./configure --prefix=/usr
  - make -k
  - make check
compiler:
  - clang
  - gcc
dist: trusty
addons:
  apt:
    packages:
      - libcryptsetup-dev
      - libssl-dev
      - libudisks2-dev
